{
  "name": "ricdom",
  "version": "0.4.4",
  "description": "JSON-based real-time control DOM library for Electron/browser",
  "license": "SEE LICENSE IN LICENSE",
  "author": "miyoshi-tec",
  "repository": {
    "type": "git",
    "url": "https://github.com/miyoshi-tec/RicDOM-v1.git"
  },
  "keywords": [
    "dom",
    "json",
    "reactive",
    "proxy",
    "ui",
    "electron",
    "lightweight"
  ],
  "main": "src/ricdom.js",
  "bin": {
    "ricdom-lz": "./scripts/build_lz_bundle.js",
    "ricdom-icon": "./scripts/icon.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/gen_version.js && npm run build:core && npm run build:ui && node scripts/build_icons.js && node scripts/update_sizes.js",
    "build:icons": "node scripts/build_icons.js",
    "build:core": "npx esbuild src/ricdom_globals.js   --bundle --minify --outfile=RicDOM.min.js   --platform=browser && node scripts/build_lz_bundle.js RicDOM.min.js RicDOM.lz.min.js",
    "build:ui": "npx esbuild src/ricui_globals.js    --bundle --minify --outfile=RicUI.min.js    --platform=browser && node scripts/shorten_css_classes.js RicUI.min.js && node scripts/compress_css_in_js.js RicUI.min.js && node scripts/build_lz_bundle.js RicUI.min.js RicUI.lz.min.js",
    "build:docs": "npm run build && node scripts/copy_docs.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^25.0.0"
  }
}
